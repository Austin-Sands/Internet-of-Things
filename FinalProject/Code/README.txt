greenhouse.py is the main script that is called by chrontab
gardenpi_init.sql is an sql file that when run will create the database needed to hold the information for the device.
The gardenpi_site folder has all of the php files for the site, css files for the site, and the python scripts to be run on site button presses